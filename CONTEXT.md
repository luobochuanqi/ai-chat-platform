# AI 探索平台

多用户 AI 对话与生图平台:管理员配置模型接入与价格,用户持点数池使用模型。

## 语言

**Provider(提供商)**:
一家模型服务方(DeepSeek、火山引擎、OpenRouter…),持有自己的 API 端点与一组 API Key。
_Avoid_: 渠道、上游、引擎

**Model(模型)**:
Provider 下可被调用的具体模型,以其 API 模型 ID 标识(`deepseek-v4-flash`、`doubao-seedream-5-0`…)。
_Avoid_: 引擎、型号

**模型池(可用池)**:
管理员启用、用户可选的 Model 集合;未入池的模型用户不可见、不可用。

**兼容协议**:
对话统一走 OpenAI 兼容 `chat/completions`,生图统一走 OpenAI 兼容 `images/generations`;平台只承诺按兼容协议接入。
_Avoid_: 原生协议、专用协议

**点数(Points)**:
平台内计价单位,非货币;按模型单价与真实用量(对话按 token、生图按张)从用户点数池扣减。
_Avoid_: 额度(历史字段名,语义已改为点数)、积分

**对话池 / 生图池**:
每个用户持有两个相互独立的点数池;对话消耗对话池,生图消耗生图池。

**全局默认模型**:
管理员设置的默认对话模型与默认生图模型;用户未在会话内切换时使用。

**会话级切换**:
用户在单个会话内从模型池选择对话/生图模型,不持久化为个人偏好。
_Avoid_: 个人默认模型
